import Component from '../classes/Component'

export default class extends Component {
  constructor ({ lerp = 0.1 }) {
    super({
      element: '.cursor',
      elements: {

      }
    })

    console.log('AEAEEA')
  }
}
